# Marketplace Backend — Starter
Minimal starter for Marketplace Backend (Node.js + TypeScript + Sequelize + MySQL)

Run locally:
1. Edit .env with your DB credentials.
2. npm install
3. npm run dev

This zip contains a tiny scaffold: src/app.ts, config, minimal models, auth controller, and seed.
